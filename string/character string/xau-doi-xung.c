#include <stdio.h> 
#include <string.h> 

int main() { 
    char str[100005]; 
    gets(str); 
    int n = strlen(str)-1; 
    printf("%d",n); 
} 

// như 1 trò đùa :)))))
// dùng làm trước khi não :))))